'use strict';
function calcSum(a, b) {
  return a + b;
}
calcSum(2, 2);
//arrow functions
let calcSumArrow; // using? as an optional
calcSumArrow = (first, second) => {
  return first + second;
};
//==================================
const sayHello = () => {
  console.log('Life is good....');
};
