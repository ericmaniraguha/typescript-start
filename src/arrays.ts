//let numbers = []; //things to avoid as this is store arrays of any type
// numbers[0] = 1;
// numbers[1] = 2;
// mnumbers[2] = '3';

let numbers: number[] = [];
numbers.forEach((n) => {
  n.toFixed;
});
